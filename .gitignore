# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.env
.venv
env/
venv/
*.pyc

# Jupyter
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# Rendered image cache — large, reproducible via render_pdf.py
data/rendered_pages/

# Model checkpoints — large, stored in GCS
checkpoints/

# Training logs — stored in GCS
logs/

# Evaluation output
eval_output/

# Temp download dir used by drive_to_gcs.py
/tmp/hallucination_pdfs_*/

# Local PDF source folders — Hebrew folder names, stored in GCS not in repo
data/pdf_s*/
data/מסמכים*/

# Sensitive / credential files — match specific secret filename patterns only
service_account*.json
credentials*.json
*_key.json

# Distribution staging packages + zipped bundles (regenerable via scripts/build_dit_package.py)
_package/
_dit_package/
*.zip

# Raw source PDFs + per-page PNGs — large, sourced from Google Drive, not committed
data/handwritten/
data/handwritten_and_questioniers/
data/handwritten_edge_cases/
data/regular_forms/
data/regular_forms_edge_cases/
data/spaciel_font/
data/spaciel_font_pages/
data/validation_set/

# Simple two-folder pipeline (ADR-0001) — raw PDFs and generated artefacts
data/safe/
data/risky/
data/rendered_pages_simple/
data/splits_simple/
data/metadata_simple.csv
data/labels_rubric_simple.csv
data/rubric_simple_checkpoint.jsonl

# Hebrew Drive archive zip
"data/פונט מוזר*"

# Split CSVs — generated, not committed
data/splits/

# macOS
.DS_Store
**/.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo
